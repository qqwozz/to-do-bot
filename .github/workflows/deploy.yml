name: Deploy

on:
  push:
    branches: [ main ]

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  deploy:
    name: Build & Deploy
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Build backend
        run: |
          cd cpp-backend
          docker build -t todo-backend:latest .

      - name: Build bot
        run: |
          docker build -t todo-bot:latest .

  # Раскомментируйте для реального деплоя:
  # deploy-production:
  #   runs-on: ubuntu-latest
  #   needs: deploy
  #   steps:
  #     - name: Deploy to server
  #       uses: appleboy/ssh-action@v1
  #       with:
  #         host: ${{ secrets.SERVER_HOST }}
  #         username: ${{ secrets.SERVER_USER }}
  #         key: ${{ secrets.SSH_KEY }}
  #         script: |
  #           cd /opt/todo-bot
  #           git pull
  #           docker-compose down
  #           docker-compose up -d --build
